# Frontend Supabase settings
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Remote MCP server settings
MCP_HOST=127.0.0.1
MCP_PORT=3333
MCP_ACCESS_TOKEN=replace-with-a-long-random-token
MCP_ALLOWED_HOSTS=localhost,127.0.0.1

# Optional: lets the MCP server read Supabase data server-side.
# Prefer the service role key only on a trusted server, never in browser code.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
